'use strict';

(function($){

	// var TB_WIDTH = 500, TB_HEIGHT = 400;
	//  // set the new width and height dimensions here..
	// $("#TB_window").animate({
	// 	marginLeft: '"' + parseInt((($(window).width()-TB_WIDTH) / 2),10) + 'px"',
	//  	width: TB_WIDTH + 'px',
	//  	height: TB_HEIGHT + 'px',
	//  	marginTop: '"' + parseInt((($(window).height()-TB_HEIGHT) / 2),10) + 'px"'
	//  });

})(jQuery);
